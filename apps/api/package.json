{
  "name": "@opensuggest/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@opensuggest/model-adapter": "file:../../packages/model-adapter",
    "@opensuggest/prompt-kit": "file:../../packages/prompt-kit",
    "@opensuggest/shared-types": "file:../../packages/shared-types",
    "dotenv": "^16.4.7",
    "fastify": "^5.2.1",
    "opensuggest999": "file:../..",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "tsx": "^4.19.3"
  }
}
